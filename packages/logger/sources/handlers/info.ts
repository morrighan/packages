// Local helpers.
import LoggingLevel from '../helpers/logging-level'
import ColorScheme from '../helpers/color-scheme'
import decorateLabel from '../helpers/label-decorator'

// Handler metadata.
export const name = 'info'
export const level = LoggingLevel.Info
export const label = decorateLabel('INFO', ColorScheme.Regular)

export default null
