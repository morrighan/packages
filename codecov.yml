flag_management:
  default_rules:
    carryforward: true

  individual_flags:
    - name: alias-mapper
      paths:
      - packages/alias-mapper

    - name: eslint-config
      paths:
      - packages/eslint-config

    - name: sass-bridge
      paths:
      - packages/sass-bridge
